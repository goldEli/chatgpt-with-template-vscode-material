import React from 'react'
import styled from '@emotion/styled'

interface <%= rawSelectedText %>Props {}

const <%= rawSelectedText %>Box = styled.div`
width: 100%;
`

const <%= rawSelectedText %>: React.FC<<%= rawSelectedText %>Props> = props => {
   return <<%= rawSelectedText %>Box><%= rawSelectedText %></<%= rawSelectedText %>Box>
}